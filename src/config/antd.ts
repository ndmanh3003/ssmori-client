import { ConfigProviderProps } from 'antd'

const antdConfig: ConfigProviderProps = {
  componentSize: 'large',
  theme: {
    token: {
      colorPrimary: '#DF4601',
      fontFamily: 'Manrope'
    }
  }
}

export default antdConfig
